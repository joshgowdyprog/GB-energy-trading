data:
  processed_data: data/processed/
model:
  model_path: models/auction_ensemble_model.pkl
  results_path: models/auction_ensemble_results.csv
strategy:
  name: baseline
  strategy_start_date: 2021-01-13 00:00:00 # start date of all available data
  strategy_end_date: 2021-11-12 23:00:00  # end date (e.g. start of training data)
  max_vol: 10 # volume per trade
  strategy_folder: strategy_results/baseline_backtest/
